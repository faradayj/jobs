# Secrets + ephemeral data
data/.env
data/jobs.db

# Build output
artifacts/
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store

# Editor + tooling
.venv/
.vscode/

# Playwright browser profiles (if created)
playwright_user_dir_*/

# Keep tracked (do NOT ignore):
# data/jobs_tracker.csv   ← source of truth for job applications
# data/jobs_details.json  ← evaluation output
# data/library.json       ← candidate profile
# data/resume.pdf         ← résumé (tracked; ask user if they prefer to ignore it)
# src/*.py                ← all source code
